// Math Operators

// Create a variable result that stores 10 + 5, then print it using console.log.
// Create a variable result that stores 20 - 8, then print it using console.log.
// Create a variable result that stores 6 * 7, then print it using console.log.
// Create a variable result that stores 40 / 5, then print it using console.log.
// Create a variable result that stores 17 % 5, then print it using console.log.
// Create a variable result that stores (10 + 5) * 2, then print it using console.log.
// Create a variable result that stores 10 + 5 * 2, then print it using console.log.
// Create a variable result that stores (20 - 4) / 2, then print it using console.log.
// Create a variable result that stores 20 - 4 / 2, then print it using console.log.
// Create a variable result that stores (8 + 2) * (6 - 3), then print it using console.log.
// Create a variable result that stores 50 % 6, then print it using console.log.
// Create a variable result that stores (15 + 5) / 4, then print it using console.log.
// Create a variable result that stores 9 * (3 + 2), then print it using console.log.
// Create a variable result that stores (100 - 25) / 5, then print it using console.log.
// Create a variable result that stores 7 + 3 * 4 - 2, then print it using console.log.
// Create a variable result that stores (7 + 3) * (4 - 2), then print it using console.log.
// Create a variable result that stores 30 / (2 + 3), then print it using console.log.
// Create a variable result that stores (18 % 5) + 10, then print it using console.log.
// Create a variable result that stores 6 * 6 / 3, then print it using console.log.
// Create a variable result that stores (25 + 15) % 7, then print it using console.log.

let one = 10 + 5;
let two = 20 - 8;
let three = 6 * 7;
let four = 40 / 5;
let five = 17 % 5;
let six = (10 + 5) / 2;
let seven = 10 + 5 * 2;
let eight = (20 - 4) / 2;
let nine = 20 - 4 / 2;
let ten = (8 + 2) * (6 - 3);
let eleven = 50 % 6;
let twelve = (15 + 5) / 4;
let thirdteen = 9 * (3 + 2);
let fourteen = (100 - 25) / 5;
let fifteen = 7 + 3 * 4 - 2;
let sixteen = (7 + 3) * (4 - 2);
let seventeen = 30 / (2 + 3);
let eighteen = (18 % 5) + 10;
let nineteen = (6 * 6) / 3;
let twenty = (25 + 15) & 7;

console.log(
  `1) ${one} \n 2) ${two} \n 3) ${three} \n 4) ${four} \n 5) ${five} \n 6) ${six} \n 7) ${seven} \n 8) ${eight} \n 9) ${nine} \n 10) ${ten} \n 11) ${eleven} \n 12) twelve ${twelve} \n 13) ${thirdteen} \n 14) ${fourteen} \n 15) ${fifteen} \n 16( ${sixteen} \n 17) ${seventeen} 18) ${eighteen} \n 19) ${nineteen} \n 20) ${twenty}`,
);
