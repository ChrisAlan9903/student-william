// 1. Create a variable called age with the value 18.
// Use an if block to print "You are 18" when age is equal to 18.

// 2. Create a variable called score with the value 80.
// Use an if block to print "Good score" when score is greater than 70.

// 3. Create a variable called temperature with the value 35.
// Use an if block to print "The weather is hot" when temperature is greater than 30.

// 4. Create a variable called number with the value 10.
// Use an if block to print "The number is positive" when number is greater than 0.

// 5. Create a variable called money with the value 50.
// Use an if block to print "You have enough money" when money is greater than or equal to 50.

// 6. Create a variable called height with the value 160.
// Use an if block to print "You can enter" when height is greater than or equal to 150.

// 7. Create a variable called passwordLength with the value 8.
// Use an if block to print "Password length is correct" when passwordLength is equal to 8.

// 8. Create a variable called total with the value 25.
// Use an if block to print "Total is more than 20" when total is greater than 20.

// 9. Create two variables called num1 and num2 with the values 5 and 3.
// Use an if block to print "The sum is 8" when num1 + num2 is equal to 8.

// 10. Create two variables called price and quantity with the values 10 and 4.
// Use an if block to print "The total price is 40" when price * quantity is equal to 40.

let age = 18;
if (age === 18) {
  console.log("You are 18");
}
let score = 80;
if (score > 70) {
  console.log("Good Score");
}
let temperature = 35;
if (temperature > 30) {
  console.log("The weather is hot");
}
let money = 50;
if (money >= 50) {
  console.log("You have enough money");
}
let height = 160;
if (height >= 150) {
  console.log("You can enter");
}
let passwordLength = 8;
if (passLength == 8) {
  console.log("Password Length is correct");
}
let total = 25;
if (total > 20) {
  console.log("Total is more than 20");
}
let num1 = 5;
let num2 = 3;
if (num1 + num2 == 8) {
  console.log("The sum is 8");
}
let price = 10;
let quantity = 4;
if (price * quantity == 40) {
  console.log("The total price is 40");
}
